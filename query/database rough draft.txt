create table user(
	userID INTEGER NOT NULL,
	userEmail VARCHAR(50) NOT NULL,
	username VARCHAR(20) NOT NULL,
	password VARCHAR(20) NOT NULL,
TODO	interests,
	firstName VARCHAR(20),
	lastName VARCHAR(20),
	PRIMARY KEY ( userID )
);

create table group(
	groupID INTEGER NOT NULL,
	groupName VARCHAR(20) NOT NULL,
TODO	interests
	PRIMARY KEY (groupID)
);

create table event(
	eventID INTEGER NOT NULL,
	eventName VARCHAR(20),
	groupID INTEGER NOT NULL,
	creator INTEGER NOT NULL,
	location VARCHAR(50) NOT NULL,
	date DATE NOT NULL,
	time TIME NOT NULL,
	dateEnd DATE NOT NULL,
	timeEnd TIME NOT NULL,
	PRIMARY KEY (eventID),
	FOREIGN KEY (groupID) REFERENCES group(groupID),
	FOREIGN KEY (creator) REFERENCES user(userID)
);

create table groupMembers(
	userID INTEGER NOT NULL,
	groupID INTEGER NOT NULL,
	isAdmin BOOL,
	PRIMARY KEY (userID, groupID),
	FOREIGN KEY (userID) REFERENCES user(userID),
	FOREIGN KEY (groupID) REFERENCES group(groupID)
);

create table eventAttend(
	userID INTEGER NOT NULL,
	eventID INTEGER NOT NULL,
	PRIMARY KEY (userID, eventID),
	FOREIGN KEY (userID) REFERENCES user(userID),
	FOREIGN KEY (groupID) REFERENCES group(groupID)
);

create table images(
	eventID INTEGER NOT NULL,
	imagePath VARCHAR(50) NOT NULL,
	PRIMARY KEY (imagePath),
	FOREIGN KEY (eventID) REFERENCES event(eventID)
);

create table kickedFromGroup(
	userID INTEGER NOT NULL,
	groupID INTEGER NOT NULL,
	PRIMARY KEY (userID, groupID),
	FOREIGN KEY (userID) REFERENCES user(userID),
	FOREIGN KEY (groupID) REFERENCES group(groupID)
);

create table bannedFromEvent(
	userID INTEGER NOT NULL,
	eventID INTEGER NOT NULL,
	PRIMARY KEY (userID, eventID),
	FOREIGN KEY (userID) REFERENCES user(userID),
	FOREIGN KEY (eventID) REFERENCES event(eventID)
);


NOTES TO CHANGE:
Remove blocked user table
Figure out messages (either use facebook thing or text field in the database)